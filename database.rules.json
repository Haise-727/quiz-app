{
  "rules": {
    "sessions": {
      ".read": true,
      ".write": true,
      "$pin": {
        ".validate": "newData.hasChildren(['quizId', 'hostId', 'state']) || !newData.exists()"
      }
    }
  }
}
